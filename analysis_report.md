# تقرير تحليل مشروع مدير عدادات المياه والوحدات السكنية

## نظرة عامة على المشروع
مشروع "مدير عدادات المياه والوحدات السكنية" هو تطبيق جوال مبني باستخدام إطار عمل Expo و React Native، مع واجهة خلفية تعتمد على tRPC وقاعدة بيانات MySQL باستخدام Drizzle ORM. يهدف التطبيق إلى إدارة الوحدات السكنية، قراءة عدادات المياه، إصدار الفواتير، وتتبع المدفوعات.

## حالة المشروع الحالية
بناءً على تحليل ملف `todo.md` وملفات الكود المصدري، يبدو أن المشروع قد أكمل المراحل الأساسية (الإعداد، قاعدة البيانات، واجهات الإدارة الأساسية)، ولكنه لا يزال قيد التطوير في بعض الميزات المتقدمة مثل تصدير الفواتير كـ PDF ونظام الإشعارات.

## المشاكل المكتشفة والتحسينات المطلوبة

### 1. مشاكل في استعلامات قاعدة البيانات (Database Queries)
في ملف `server/db.ts`، هناك بعض المشاكل في كيفية إرجاع البيانات من الاستعلامات التي تستخدم `innerJoin`:
- **استعلام `getUserInvoices`**: يرجع البيانات مجمعة (Joined) بدلاً من شكل الفاتورة المسطح (Flat). هذا يعني أن الواجهة الأمامية قد تتوقع `invoice.id` ولكنها تتلقى `{ invoices: { id: ... }, units: { ... } }`.
- **استعلام `getUserPayments`**: يعاني من نفس المشكلة، حيث يرجع بيانات مجمعة من جداول `payments`، `invoices`، و `units`.
- **استعلام `getUserStatistics`**: يقوم بجلب جميع العدادات والفواتير في النظام ثم يقوم بتصفيتها في الذاكرة (In-memory filtering) بناءً على `unitIds`. هذا غير فعال وقد يؤدي إلى مشاكل في الأداء مع زيادة حجم البيانات. يجب استخدام استعلامات SQL المباشرة للتصفية.

### 2. مشاكل في واجهة قراءة العدادات (`readings.tsx`)
بناءً على التحليل، يبدو أن شاشة قراءة العدادات لا تستخدم البيانات الحقيقية للعدادات من قاعدة البيانات. بدلاً من ذلك، تقوم بإنشاء قائمة وهمية (Synthetic) للعدادات بناءً على الوحدات السكنية. هذا يعني أن القراءات المدخلة قد لا ترتبط بالعدادات الصحيحة في قاعدة البيانات، مما يؤدي إلى أخطاء في السجل التاريخي وحساب الاستهلاك.

### 3. مشاكل في واجهة تفاصيل الفاتورة (`invoice-detail.tsx`)
- **تصدير PDF وإرسال SMS**: الدوال المسؤولة عن هذه العمليات (`handleExportPDF` و `handleSendSMS`) هي مجرد دوال وهمية (Placeholders) تعرض رسائل نجاح دون تنفيذ أي وظيفة حقيقية.
- **تاريخ الإصدار (`issuedDate`)**: الواجهة تحاول عرض حقل `issuedDate` من الفاتورة، ولكن هذا الحقل غير موجود في مخطط قاعدة البيانات (`schema.ts`). المخطط يحتوي فقط على `createdAt` و `dueDate`.
- **زر تسجيل الدفع**: الزر موجود في الواجهة ولكنه لا يحتوي على أي إجراء (Action) عند النقر عليه.

### 4. مشاكل في واجهة التقارير (`reports.tsx`)
- تعتمد الواجهة على حقل `issuedDate` غير الموجود في قاعدة البيانات لتجميع البيانات الشهرية.
- زر تصدير التقرير لا يحتوي على أي إجراء.

### 5. مشاكل في واجهة الإعدادات (`settings.tsx`)
- جميع الإعدادات يتم حفظها محلياً في حالة المكون (Component State) فقط، ولا يتم حفظها في قاعدة البيانات أو `AsyncStorage` بشكل دائم.
- زر تسجيل الخروج لا يحتوي على أي إجراء.

## التوصيات والخطوات القادمة

1. **إصلاح استعلامات قاعدة البيانات**: تعديل الاستعلامات في `server/db.ts` لإرجاع البيانات بالشكل الصحيح الذي تتوقعه الواجهة الأمامية، وتحسين استعلام الإحصائيات ليكون أكثر كفاءة.
2. **ربط واجهة قراءة العدادات بالبيانات الحقيقية**: تعديل `readings.tsx` لجلب العدادات الحقيقية المرتبطة بالوحدات من قاعدة البيانات بدلاً من إنشائها وهمياً.
3. **إصلاح نموذج بيانات الفاتورة**: تحديث الواجهات التي تعتمد على `issuedDate` لاستخدام `createdAt` أو إضافة حقل `issuedDate` إلى قاعدة البيانات إذا كان مطلوباً.
4. **تفعيل الأزرار والإجراءات المفقودة**: إضافة الوظائف الحقيقية لأزرار الدفع، التصدير، وتسجيل الخروج.
5. **تنفيذ ميزات التصدير والإشعارات**: البدء في تنفيذ الميزات المتبقية في `todo.md` مثل توليد ملفات PDF وإرسال الإشعارات.

هذا التقرير يغطي المشاكل الأساسية التي تم اكتشافها في الكود الحالي. يمكنك البدء في معالجة هذه المشاكل خطوة بخطوة لتحسين استقرار التطبيق ووظائفه.
